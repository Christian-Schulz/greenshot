<UserControl x:Class="Greenshot.Addon.Redmine.Views.RedmineConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Greenshot.Addon.Redmine.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:RedmineConfigViewModel,IsDesignTimeCreatable=False}"
             >
    <StackPanel>
        <GroupBox Header="{Binding RedmineLanguage.LabelRedmine}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ContentControl x:Name="FileConfigPartViewModel" Grid.Row="0"/>
                <DockPanel Grid.Row="1" LastChildFill="True">
                    <Label Content="{Binding RedmineLanguage.LabelUrl}" Width="100" />
                    <TextBox Text="{Binding RedmineConfiguration.Url}" />
                </DockPanel>
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>
